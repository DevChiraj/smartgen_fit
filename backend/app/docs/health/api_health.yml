tags:
  - Health
summary: API liveness check
description: Returns 200 if the Flask process is up. Does not touch the database.
responses:
  200:
    description: Service is up
    schema:
      type: object
      properties:
        status:
          type: string
          example: ok
        service:
          type: string
          example: smartgen-fit-api
