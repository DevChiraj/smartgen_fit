tags:
  - Meal Logs
summary: Get the logged-in user's meal log history
description: Most recent 100, newest first.
security:
  - BearerAuth: []
responses:
  200:
    description: Meal log history
    schema:
      type: object
      properties:
        history:
          type: array
          items:
            $ref: '#/definitions/MealLog'
  401:
    description: Missing/invalid/expired access token
